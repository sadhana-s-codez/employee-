Employee Data Scraper

This project fetches employee data from the provided API endpoint and extracts the required fields from the JSON response, including Employee ID, First Name, Last Name, Email, Job Title, and Phone Number etc.

Requirements

* Python 3.x
* requests

Run the project

pip install -r requirements.txt

python employee.py